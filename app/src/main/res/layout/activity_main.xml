<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.coulcod.oauth.MainActivity">

    <Button
        android:id="@+id/btnFacebook"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/through_facebook"
        android:onClick="onBtnFacebookClick"
        />

    <Button
        android:id="@+id/btnGooglePlus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/through_googleplus"
        android:layout_below="@id/btnFacebook"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:onClick="onBtnGooglePlusClick"
        />

    <Button
        android:id="@+id/btnTwitter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/through_twitter"
        android:layout_below="@id/btnGooglePlus"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:onClick="onBtnTwitterClick"
        />

</RelativeLayout>
